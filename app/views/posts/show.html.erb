<p id="notice"><%= notice %></p>


 <h1><u><%= @post.post_title %></u></h1></br>
 <% @tags.each do |tag| %>
     <%=link_to "#{tag.tag_text}", "/tags/#{tag.tag_text}" %>
 <% end %>
 </br>
 </br>
 </br>
 <%= @post.post_text %></br>
 <%= @post.post_date %></br>
 <ol>
     <% @comments.each do |comment| %>
        <li><%=comment.comment_text  %></li>
     <%end %>
</ol>

 <% if current_user != nil %>
 <%= link_to "Create Comment", controller: "comments", action: "new" %>
 <% end %>

</br>

<%= link_to 'Back', posts_path %>
